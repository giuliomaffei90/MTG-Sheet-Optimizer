---
name: build
description: Lancia lo script di build dev di MTG Sheet Optimizer (scripts/build_dev_mac.sh) per fare una build Release, chiudere l'istanza in esecuzione e rilanciare la build appena creata. Attivala quando l'utente scrive "/build" o chiede di buildare/compilare l'app.
---

Esegui, senza commenti né riepilogo:

```bash
./scripts/build_dev_mac.sh
```
